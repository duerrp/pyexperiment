language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libhdf5-serial-dev 1.8.4-patch1-3ubuntu2
install: "pip install -r requirements.txt"
# command to run tests
script: run_tests